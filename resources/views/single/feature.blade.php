<div class="featureElement d-flex gap-2 align-items-center">
    <img src="{{ Vite::asset("/resources/img/".$featureImageName) }}" alt="{{ $featureTitle }}">
    <span class="h-100 fw-semibold text-uppercase">{{ $featureTitle }}</span>
</div>
